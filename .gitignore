# Local machine paths — carries the operator's username. Never published.
config.local

# Queued/sent payloads are working state, not source.
payload_*.json
sent.log
last-run.log

# bytecode cache, appears when check.py is imported rather than run
__pycache__/
